#!/usr/bin/env ruby

require 'bundler'
Bundler.require

require File.expand_path("../../lib/hangman", __FILE__)

game = Hangman.new

user_answer = game.greeting
if user_answer == "n" || user_answer == "no"
  game.no_game
else
  game.play!
end
